// Function to simulate navigation
function navigateTo(page) {
    alert(`Navigating to: ${page}`);
    // Use window.location.href to navigate to the desired page
    window.location.href = page;  // This will change the browser's URL and load the new page
}
